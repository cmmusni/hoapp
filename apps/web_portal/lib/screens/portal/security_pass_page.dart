import 'package:flutter/material.dart';
import 'package:core_ui/core_ui.dart' as shared;

class SecurityPassPage extends StatelessWidget {
  const SecurityPassPage({super.key});

  @override
  Widget build(BuildContext context) => const shared.SecurityPassScreen();
}
