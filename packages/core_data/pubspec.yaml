name: core_data
description: Data layer with repositories and API clients for HOApp
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core_domain:
    path: ../core_domain
  supabase_flutter: ^2.3.4
  http: ^1.1.2
  shared_preferences: ^2.2.2
  file_picker: ^6.1.1
  image_picker: ^1.0.7
  mime: ^1.0.4
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8
  fake_async: ^1.3.1
